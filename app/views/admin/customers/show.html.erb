<p id="notice"><%= notice %></p>
<div class="container">
  <div class="row">
    <div class="col-sm-8 mt-4 px-sm-0 offset-sm-1">
      <h2><%= @customer.last_name + @customer.first_name %>さんの会員詳細</h2>
    </div>

    <div class="col-sm-8 mt-4 px-sm-0 offset-sm-3">
      <div class="row">
        <table>
          <tr>
            <th>会員ID</th>
            <td><%= @customer.id %></td>
          </tr>
          <tr>
            <th>氏名</th>
            <td><%= @customer.last_name + "　" + @customer.first_name %></td>
          </tr>
          <tr>
            <th>フリガナ</th>
            <td><%= @customer.last_name_kana + "　" + @customer.first_name_kana %></td>
          </tr>
          <tr>
            <th>郵便番号</th>
            <td><%= @customer.postal_code %></td>
          </tr>
          <tr>
            <th>住所</th>
            <td><%= @customer.address %></td>
          </t>
          <tr>
            <th>電話番号</th>
            <td><%= @customer.phone_number %></td>
          </tr>
          <tr>
            <th class="pr-5">メールアドレス</th>
            <td><%= @customer.email %></td>
          </tr>
          <tr>
            <th>会員ステータス</th>
            <!--true=退会済み、false=有効-->
              <% if @customer.is_status == true %>
                <td class="text-secondary">退会</td>
              <% else %>
                <td class="text-success">有効</td>
              <% end %>
          </tr>
        </table>
      </div>
    </div>
    <div class="col-sm-8 mt-4 px-sm-0 offset-sm-3">
      <div class="row mx-auto">
        <div class="actions">
          <%= link_to "編集する", edit_admin_customer_path, class: "btn btn-sm btn-success" %>
        </div>

        <div class="actions ml-5">
          <%= link_to "注文履歴一覧を見る", admin_orders_path, class: "btn btn-sm btn-primary" %>
        </div>
      </div>
    </div>

  </div>
</div>










