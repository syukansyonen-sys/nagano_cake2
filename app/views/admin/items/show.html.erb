<h2>商品詳細</h2>

<h4>商品画像</h4>
  <%= f.file_field :image, accept: "image/*" %>

  <h4>商品名</h4>
  <%= @item.name %>

  <h4>商品説明</h4>
  <%= @item.introduction %>

  <h4>ジャンル</h4>

  <h4>税込価格（税抜価格）</h4>
  <%= f.text_field :price %>

  <h4>販売ステータス</h4>

  <%= link_to "編集する", edit_admin_item_path(item.id) %>
